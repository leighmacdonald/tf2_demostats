# Generated by Cargo
# will have compiled files and executables
debug/
target/

# NOTE: Cargo.lock is intentionally tracked: the nix package build needs it
# (see flake.nix `cargoLock.lockFile`). Keep it committed for the binary.

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb


/.idea

*.dem
/dist/
.direnv
.env
*.dem
*.json
